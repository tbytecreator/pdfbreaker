Quebra um pdf em dados estruturados (JSON). 

#Instale as dependências: 
pip install -r requirements.txt
pip install PyPDF2

#Uso
cd /home/tbytecreator/dev/tbytecreator/pdfbreaker/pdf-struct-extractor/src
python -m pdf_struct.cli ./teste.pdf
